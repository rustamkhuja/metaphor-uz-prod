const params=new URLSearchParams(location.search);const partner=(params.get('partner')||'public').replace(/[^a-zA-Z0-9_-]/g,'').slice(0,40);const cid=`widget:${partner}:${localStorage.getItem('metaphor_widget_id')||crypto.randomUUID()}`;localStorage.setItem('metaphor_widget_id',cid.split(':').pop());const form=document.getElementById('wform');let text='';form.onsubmit=async e=>{e.preventDefault();const b=form.querySelector('button');b.disabled=true;b.textContent='Создаю…';try{const fd=new FormData(form);const r=await fetch('/api/v1/generate',{method:'POST',headers:{'Content-Type':'application/json','X-Client-Id':cid},body:JSON.stringify({mode:'write',language:document.getElementById('wlang').value,relationship:fd.get('relationship'),goal:'congratulate',tone:fd.get('tone'),output_format:'card',length:'medium',context:fd.get('context'),source_text:'',recipient_name:'',address_form:'auto',source:'widget',partner_code:partner,tier:'free',accepted_terms:true})});const d=await r.json();if(!r.ok)throw new Error(d.detail||'Ошибка');text=d.variants[0].text;document.getElementById('wtext').textContent=text;document.getElementById('wresult').classList.remove('hidden');parent.postMessage({type:'metaphor:generated',partner,generationId:d.generation_id},'*')}catch(err){alert(err.message)}finally{b.disabled=false;b.textContent='Создать текст карточки'}};document.getElementById('wcopy').onclick=()=>navigator.clipboard.writeText(document.getElementById('wtext').innerText);
